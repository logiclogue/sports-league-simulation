let split xs =
    match xs with
    | [] -> [], []
    | [x] -> [x], []
    | [x; y] -> [x], [y]
    | [x; y; z] -> [x; y], [z]

let equalise xys =
    [], []

let zip_opt x_opts y_opts =
    []

let rotate xs =
    []
